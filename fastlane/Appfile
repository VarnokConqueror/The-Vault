app_identifier(ENV.fetch("APP_BUNDLE_IDENTIFIER", "com.example.courtMobile"))

team_id(ENV["APPLE_DEVELOPMENT_TEAM"]) if ENV["APPLE_DEVELOPMENT_TEAM"]
itc_team_id(ENV["APP_STORE_CONNECT_TEAM_ID"]) if ENV["APP_STORE_CONNECT_TEAM_ID"]
apple_id(ENV["APPLE_ID"]) if ENV["APPLE_ID"]
